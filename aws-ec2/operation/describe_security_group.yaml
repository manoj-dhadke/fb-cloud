---
title: "Describe Security group in Amazon EC2"
description: "A security group is for use with instances either in the EC2-Classic platform or in a specific VPC."

flintbit: "fb-cloud:aws-ec2:operation:describe_security_group.rb"
icon: "fa fa-user"
inputs:
  -
    section: Basic Fields
    fields:
      -
        default: "amazon-ec2"
        id: connector_name
        label: Flint's Amazon EC2 connector name
        placeholder: "Enter amazon EC2 connector name"
        type: textbox
        required: true
      -
        default:
        id: group_name
        label: Security Group Name
        placeholder: "Enter Security Group Name"
        type: textbox
        required: true
      -
        default: "us-east-1"
        id: region
        label: Region
        placeholder: "Amazon EC2 Region"
        type: select
        required: true
        options:
          - ap-southeast-1
          - ap-southeast-2
          - eu-central-1
          - ap-northeast-2
          - ap-northeast-1
          - us-east-1
          - sa-east-1
          - us-west-1
          - us-west-2
      -
        default:
        id: access-key
        label: Access Key
        placeholder: "Access Key"
        type: textbox
        required: false
      -
        default:
        id: security-key
        label: Security Key
        placeholder: "Security Key"
        type: password
        required: false
      -
        default: 60000
        id: timeout
        label: Timeout
        placeholder: "timeout"
        type: integer
        required: false
...
