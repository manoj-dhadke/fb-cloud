---
title: "Reboot DigitalOcean droplet"
description: "Reboot droplet from DigitalOcean data center"

flintbit: "fb-cloud:digitalocean:operation:reboot_instance.rb"
icon: "fa fa-user"
inputs:
  -
    section: Reboot Droplet
    fields:
        default:
        id: id
        label: droplet ID
        placeholder: "Id of the droplet"
        type: textbox
        required: true
...
