---
title: "IBM Softlayer:Delete Instance"
description: "Delete server instance from IBM Softlayer data center"

flintbit: "fb-cloud:softlayer:operation:delete_instance.rb"
icon: "fa fa-close (alias)"
inputs:
  -
    section: Delete Instance
    fields:
      -
        default:
        id: id
        label: Instance ID
        placeholder: "ID of IBM Softlayer Instance"
        type: textbox
        required: true
...
