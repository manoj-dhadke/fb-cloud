---
title: "Update IBM Softlayer Instance"
description: "Update instance of IBM Softlayer"

flintbit: "fb-cloud:softlayer:operation:update_instance.rb"
icon: "fa fa-user"
inputs:
  -
    section: Update Instance
    fields:
      -
        default:
        id: id
        label: Instance ID
        placeholder: "ID of IBM Softlayer Instance"
        type: textbox
        required: true
      -
        default:
        id: host-name
        label: Hostname
        placeholder: "Hostname of Instance"
        type: textbox
        required: false
      -
        default:
        id: domain-name
        label: Domain Name
        placeholder: "Domain Name of Instance"
        type: textbox
        required: false
      -
        default:
        id: full-name
        label: Full Name
        placeholder: "Name of Instance"
        type: textbox
        required: true
...
