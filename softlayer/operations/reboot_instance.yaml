---
title: "IBM Softlayer:Reboot Instance"
description: "Reboot instance of IBM Softlayer"

flintbit: "fb-cloud:softlayer:operation:reboot_instance.rb"
icon: "fa fa-refresh"
inputs:
  -
    section: Reboot Instance
    fields:
      -
        default:
        id: id
        label: Instance ID
        placeholder: "ID of IBM Softlayer Instance"
        type: textbox
        required: true
...
